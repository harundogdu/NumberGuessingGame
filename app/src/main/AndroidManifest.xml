<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.info.sayitahminet">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_icon_resmi"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_icon_resmi_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SayiTahminEt">
        <activity android:name=".SonucActivity"></activity>
        <activity android:name=".TahminActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>